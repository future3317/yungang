# 市场与卡牌规则

公开市场维持三张文化证据。探索消耗 1 AP，把选中的证据收入当前角色手牌；市场由服务端补牌并使用旅程种子保持可复现。文化证据可贡献给地点任务、用于项目推进、产生即时效果或交换。

手牌达到上限时，探索不会消失；服务端进入 `pending_choice: discard`，玩家先选择放下哪件线索，再完成新线索的探索。策略牌独立放在 `action_hand`，需要服务端返回的目标后才会结算。
