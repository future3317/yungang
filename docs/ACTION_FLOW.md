# 行动流程

普通浏览不会消耗 AP。玩家选择行动后，服务端返回合法目标，地图和目标清单只突出这些目标；点击目标先进入 `ActionPreview`，确认后才提交。行动请求携带 `expected_revision`，服务端原子校验版本、席位、目标、成本和规则。

探索、贡献、修护、路线勘察、路线修护、连接、策略牌和角色能力均沿同一确认路径。路线行动使用 `route_id`，不会用相邻节点猜路线。Escape 和取消按钮会清空旧选择并回到浏览状态。
