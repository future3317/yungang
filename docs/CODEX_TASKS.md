# v2 执行任务清单

## 已完成

- [x] 阅读并拆解 `CODEX_REFACTOR_PROMPT.md`。
- [x] 备份 v1 机制与旧数据到 `docs/legacy/`、`data/legacy/`。
- [x] 建立分层内容文件和 `content_schema_version=2`。
- [x] 建立遗产节点网络、AP、探索、修复、贡献、文化牌和事件核心模型。
- [x] 建立后端唯一规则源与 SQLite 状态持久化。
- [x] 建立 `expected_revision` 并发防重放边界。
- [x] 重写原生前端为节点网络界面，并保留可折叠 JSON 调试状态。
- [x] 更新机制、学习、架构、迁移和 UI 调试文档。

## 下一轮平衡实验

- [ ] 明确团队胜利阈值与威胁失败阈值。
- [ ] 为每个角色补齐可验证的主动技能和被动能力。
- [ ] 扩展路线分支、事件选择和 `pending_choice` 流程。
- [ ] 增加多人会话创建、玩家加入和权限校验。
- [ ] 补充 v2 API、规则引擎和浏览器交互测试。
- [ ] 更新截图审计到节点网络关键视图。

仓库没有 `.git`，本次无法创建 git checkpoint；文件级旧版本备份已保留。
