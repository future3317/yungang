# 错误模型

API 错误统一返回 `detail.code`、`detail.message`、`detail.details` 和 `detail.recovery`。常见恢复包括回到首页、返回房间、等待当前席位、同步最新状态、选择其他目标和打开结果页。

`400` 表示行动或输入不合法，`401` 表示席位凭证无效，`403` 表示房主或当前席位权限不足，`404` 表示房间或旅程不存在，`409` 表示 revision 冲突、房间未开始、暂停或席位未准备。前端优先显示中文 message，不展示内部 ID 或堆栈。
