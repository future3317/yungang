# Codex 项目重构指令：将《云冈飞天棋》重构为非飞行棋文创游戏

你是本项目的主程、系统策划和前端交互工程师。请直接在当前仓库中完成一次可运行、可测试、可演示的机制重构，不要只输出建议或伪代码。

## 一、开始前必须完成的仓库审查

先完整阅读并核对以下文档与实际代码，实际路径若有差异，以仓库为准：

- `ARCHITECTURE.md`
- `ART_UI_AUDIT_REPORT.md`
- `CODEX_TASKS.md`
- `MECHANICS.md`
- `MECHANICS_REDESIGN_OPTIONS.md`
- 后端入口、状态存储、数据库代码
- `data/game_data.json`
- `frontend/index.html`
- `frontend/static/js/app.js`
- `frontend/static/css/styles.css`
- 现有测试与截图审计脚本

先输出一份简短的“仓库现状与影响面清单”，但不要停在计划阶段；随后继续实际修改代码。

在修改前创建一个可回退的 Git 检查点。若当前环境允许提交，创建提交：

`chore: checkpoint legacy flying-chess prototype`

若无法提交，至少不要删除旧文档；把旧规则移动或复制到 `docs/legacy/`，并说明回退方法。

## 二、重构目标

把当前“掷骰子—沿 48 格前进—落点结算”的飞行棋骨架，重构为一个明确的非飞行棋玩法：

**暂定名称：**《云冈文明共建录：文明互鉴之路》

**玩法类型：**2–4 人、全合作、遗产节点网络、行动点决策、任务组合、事件压力。

**建议时长：**25–40 分钟。

**体验目标：**

1. 玩家每回合都能做有意义的选择，不再由骰子决定主要行动。
2. “文明互鉴”必须通过交换、组合、共同贡献等机制被玩家实际执行，而不是只通过说明文字表达。
3. “遗产保护”必须成为需要权衡的共同任务，而不是单一进度条。
4. 保留现有云冈视觉资产、遗产节点、四个角色、五类文明印记、文化卡牌、共同图卷和日志系统。
5. 首版继续使用 FastAPI + 原生 HTML/CSS/JavaScript + JSON + SQLite。不要在本次机制重构中同时迁移 React/Vite，不要引入前端构建链。
6. 服务端必须是唯一规则权威，前端只渲染服务端返回的状态、合法动作和可选目标。
7. 不得编造文化史实、文物信息、出处或版权信息。仓库没有可靠资料时使用明确的 `TODO_CONTENT_REVIEW` 占位，并保留内容审核字段。

## 三、非目标

本次不要实现：

- WebSocket 在线多人
- 复杂 AI 玩家
- React/Vue 重写
- AR、语音、账号系统
- 商业化支付
- 同时保留两套可玩的完整机制
- 大规模新增未经审核的历史文案

旧版飞行棋只作为 Git 历史或 `docs/legacy/` 文档保留，不作为主界面可选模式。

## 四、新版核心玩法规格

### 4.1 棋盘：遗产节点网络，而不是顺序赛道

删除或停用 48 格双环顺序路线、骰子和“位置索引前进”规则。

使用六个遗产节点构成网络：

- 云冈石窟
- 华严寺
- 善化寺
- 长城关隘
- 互市驿站
- 平城遗址

推荐使用“车轮式网络”：

- 云冈石窟位于中央，与中央佛像视觉合并。
- 其余五个节点分布在外围。
- 中央与外围节点相连。
- 外围相邻节点可以按数据中的 `connections` 建立部分连线。
- 玩家只能沿明确的连接边移动。
- 四个角色使用 `start_site_id` 指定起始节点，不再使用 `start_index`。

不要把外围节点再画成一圈顺序格；视觉上必须明确是网络地图。

### 4.2 回合与轮次

每一轮开始时揭示一张全局事件卡，并显示事件将在本轮结束时产生的影响，使玩家可以提前应对。

每名玩家依次获得 **3 个行动点 AP**。玩家可在合法条件下自由组合行动，也可以提前结束回合。

玩家行动：

1. `move`：沿一条连接边移动到相邻遗产节点，消耗 1 AP。
2. `explore`：在当前节点从公开文化卡市场中选择 1 张符合规则的卡加入手牌，消耗 1 AP。
3. `contribute`：把 1 张手牌投入当前节点的文化任务，消耗 1 AP。
4. `restore`：消耗 1 AP 和相应共同修护资源，使当前节点损伤值 -1。
5. `exchange`：与同一节点的另一名玩家交换或赠予 1 张手牌，消耗 1 AP。
6. `use_skill`：使用本角色每回合一次的主动技能；技能是否消耗 AP 由数据配置。
7. `end_turn`：提前结束当前玩家回合。

一个玩家用完 AP 或主动结束后，轮到下一位玩家。最后一名玩家结束后，服务端执行本轮事件结算、检查损伤与失败条件、进入下一轮并揭示新事件。

### 4.3 文化卡市场与手牌

保留现有文化卡牌体系，但把卡牌从“随机即时效果”为主，调整为“文化证据 + 战术效果”的双用途结构。

- 公开市场始终展示 3 张文化卡。
- `explore` 时玩家从市场选择 1 张，随后补充市场。
- 手牌上限沿用 3 张；超过上限时必须进入服务端驱动的弃牌选择状态，不能由前端静默丢弃。
- 卡牌至少包含：
  - `id`
  - `name`
  - `domain`：`statue | pattern | architecture | border | trade`
  - `origin_tags`
  - `site_tags`
  - `effect`
  - `culture_note`
  - `mutual_learning_note`
  - `source_title`
  - `source_url`
  - `copyright_note`
  - `content_review_status`

`content_review_status` 至少支持：

- `verified`
- `needs_review`
- `placeholder`

无可靠资料时不得生成看似真实的来源。

### 4.4 节点文化任务

每个遗产节点拥有一个当前任务。任务不是问答弹窗，而是需要玩家组合卡牌标签完成的共同目标。

任务数据至少包含：

- `id`
- `site_id`
- `name`
- `required_domains`
- `required_origin_diversity`
- `required_card_count`
- `reward`
- `learning_objective`
- `culture_explanation`
- `source_*`
- `content_review_status`

建议首版规则：

- 一个任务需要贡献 2–3 张卡。
- 至少包含两个不同文明领域或来源标签，体现“互鉴”而非单一收集。
- 所有玩家均可向同一任务贡献卡牌。
- 贡献持续保留，直到任务完成或事件清除。
- 任务完成后：
  - 对应文明印记在共同图卷上点亮；
  - 共同图卷进度 +2；
  - 当前节点获得一次修护收益或共同修护资源；
  - 弹出简洁的文化解释与资料来源；
  - 记录每名玩家的贡献，用于结算称号。

不要恢复“个人集齐五印记即获胜”的规则。新版应以全队共同胜负为主，个人贡献只用于结算称号和复盘，不产生与共同目标冲突的个人胜利。

### 4.5 共同资源与节点损伤

把原“保护值”重命名为更符合语义的 **共同修护资源**，仍可沿用 0–10 数值范围。

每个节点拥有：

- `damage`
- `max_damage`
- `status`：`stable | at_risk | closed`
- `active_task_id`
- `contributions`

建议普通难度：

- 共同修护资源初始 6/10。
- 节点 `damage` 初始为 0 或由难度配置决定。
- 节点达到 `max_damage = 3` 时关闭。
- 关闭节点不可探索、贡献或正常进入；特殊技能和卡牌可例外。
- 两个节点关闭时全队失败。
- 完成任务、特定卡牌和角色技能可以补充共同修护资源。

不要让损伤只存在于日志；节点卡面和地图必须有清晰的 0/3 状态反馈。

### 4.6 事件系统

新增可数据驱动的事件牌堆。

事件至少包含：

- `id`
- `name`
- `description`
- `target_rule`
- `damage`
- `modifier`
- `difficulty_weight`
- `culture_note`
- `source_*`
- `content_review_status`

首版事件优先使用不依赖复杂史实判断的主题：

- 风沙侵蚀
- 岩体风化
- 道路阻断
- 修护倡议
- 工匠协作
- 互市往来

事件必须“先揭示、后结算”，给玩家一轮时间响应。避免不可预见、无法应对的纯惩罚。

### 4.7 四个角色重做

保留角色名称、美术和文化身份，但移除所有依赖骰子或特定旧格子的能力。

实现为每回合一次的主动技能，建议：

- **平城工匠**：`精修`——在当前节点执行一次加强修护；用 1 AP 移除 2 点损伤，资源消耗按普通修护一次计算。
- **西域乐舞使者**：`和鸣`——本回合贡献卡牌时，可将 1 张卡额外视为一种不同的 `origin_tag`，用于满足互鉴多样性要求。
- **草原骑行使者**：`疾行`——本回合第一次移动可跨越最多两条连续连接边，只消耗 1 AP，但中间节点不可为关闭状态。
- **中原文书记官**：`博览`——从文化市场和牌堆顶部构成的候选中查看 3 张，选择 1 张加入手牌，其余按确定性规则放回；消耗 1 AP。

技能名称、数值和是否消耗 AP 放入 JSON，不要硬编码在前端。

### 4.8 胜负条件

普通难度建议：

全队胜利：

- 在第 8 轮事件结算前点亮五类共同文明印记；
- 共同图卷达到 10/10；
- 关闭节点少于 2 个。

全队失败：

- 两个遗产节点关闭；或
- 第 8 轮结束后仍未满足胜利条件。

结算页面显示：

- 全队结果
- 完成的任务与文化知识摘要
- 各玩家贡献统计
- 非排他性称号，例如“修护主力”“文明桥梁”“行旅先锋”“典籍整理者”
- 本局出现的文化卡与资料来源列表

### 4.9 难度

数据化提供：

- `easy`：10 轮、初始修护资源 8、事件较弱。
- `normal`：8 轮、初始修护资源 6。
- `hard`：7 轮、初始修护资源 5、事件目标和伤害更强。

首屏创建游戏时允许选择难度。

## 五、学习目标与机制映射

新增 `docs/LEARNING_MECHANICS_MAP.md`，明确以下映射：

| 学习目标 | 游戏机制 |
|---|---|
| 识别造像、纹样、建筑、边塞、互市五类文化要素 | 卡牌领域标签、任务需求、共同图卷五印记 |
| 理解不同文化来源之间的交流与融合 | 任务要求不同 `origin_tags` 共同贡献 |
| 理解遗产保护需要资源、优先级和协作 | 节点损伤、事件预告、修护行动、共同资源 |
| 促进玩家讨论与协作表达 | 同节点交换、共同任务、角色互补 |
| 形成可复盘的文化记忆 | 任务完成弹窗、来源字段、终局文化摘要 |

任何文化知识点都必须至少对应一个玩家行为或可观察决策，不能只作为装饰性长文本存在。

## 六、后端重构要求

### 6.1 领域逻辑分层

检查当前仓库结构后，以最小破坏方式把纯游戏逻辑从 FastAPI 路由中拆出。建议模块：

- `models`：Pydantic/数据模型、枚举
- `engine`：状态转换、回合与轮次
- `actions`：各动作校验与执行
- `effects`：卡牌、事件、角色技能效果
- `content`：JSON 加载与校验
- `repository`：内存/SQLite 存储适配

不要为了追求目录整齐进行无关的大规模重命名。

### 6.2 统一动作接口

保留：

- `GET /api/meta`
- `POST /api/games`
- `GET /api/games/{session_id}`（若当前没有则补充）

新增或重构为：

`POST /api/games/{session_id}/actions`

请求体示例：

```json
{
  "type": "move",
  "expected_revision": 12,
  "payload": {
    "target_site_id": "huayan_temple"
  }
}
```

动作类型：

- `move`
- `explore`
- `contribute`
- `restore`
- `exchange`
- `use_skill`
- `discard`
- `end_turn`

服务端响应必须包含完整最新状态，并包含：

- `revision`
- `phase`
- `current_player_id`
- `actions_remaining`
- `legal_actions`
- `available_targets`
- `pending_choice`
- `current_event`
- `next_event_preview`（若难度允许）
- `logs`
- `win_state`

前端不得复制一套规则来猜测按钮是否可用。

### 6.3 防重复提交和并发

状态增加单调递增的 `revision`。

每次动作请求携带 `expected_revision`。版本不匹配时返回 HTTP 409，并返回最新 revision 或可重新获取状态的明确信息。

前端仍须提供全局 `isLoading` 锁、按钮禁用态和错误恢复。后端 revision 是最终保护。

### 6.4 确定性与随机数

牌堆洗牌和事件目标选择使用可注入、可记录 seed 的随机数生成器。

创建游戏允许测试环境传入 seed。生产环境自动生成 seed 并保存到游戏状态。

所有测试不得依赖不可控的全局随机数。

### 6.5 状态模型建议

根据实际项目调整，但至少覆盖：

```json
{
  "schema_version": 2,
  "mode": "heritage_network",
  "revision": 0,
  "seed": 12345,
  "difficulty": "normal",
  "round": 1,
  "max_rounds": 8,
  "phase": "player_actions",
  "current_player_index": 0,
  "actions_remaining": 3,
  "shared": {
    "restoration_resource": 6,
    "restoration_resource_max": 10,
    "scroll_progress": 0,
    "scroll_goal": 10,
    "completed_domains": []
  },
  "sites": {},
  "players": [],
  "decks": {
    "culture_draw": [],
    "culture_discard": [],
    "culture_market": [],
    "event_draw": [],
    "event_discard": []
  },
  "current_event": null,
  "next_event_preview": null,
  "pending_choice": null,
  "win_state": null,
  "logs": []
}
```

### 6.6 存档迁移

旧版位置索引无法可靠映射到新版节点，不要静默转换。

- 增加 `schema_version`。
- 启动时识别旧版 SQLite 状态。
- 对旧存档返回“该存档属于旧版机制，请新建游戏”的明确结果，或将其标记为只读 legacy。
- 修改数据库前创建备份或提供迁移脚本。
- 新增 `docs/MIGRATION_V1_TO_V2.md`。

## 七、JSON 内容结构重构

把原有 `game_data.json` 备份为 legacy 文件，随后可拆分为：

- `data/sites.json`
- `data/routes.json`
- `data/roles.json`
- `data/culture_cards.json`
- `data/tasks.json`
- `data/events.json`
- `data/difficulty.json`

如果当前加载器强依赖单文件，可以先保留单文件，但必须按顶级键清晰分区并增加 `content_schema_version`。

所有数据在应用启动时进行完整校验：

- ID 唯一
- 引用存在
- 连接边合法
- 任务需求合法
- 卡牌标签合法
- 难度数值合法
- 来源与审核状态字段存在

内容错误应在启动或测试时暴露，不要到玩家操作时才报错。

## 八、前端重构要求

### 8.1 视觉布局

保留：

- 暖博物馆色系
- 中央佛像 medallion
- 遗产节点徽章
- 四个角色徽章
- 横版卡牌
- 共同图卷
- 日志和文化弹窗

删除或替换：

- 骰子组件
- 48 个顺序棋格
- “掷骰—前进”阶段指示
- 基于 route index 的棋子定位

新版主区域：

- 左侧或中央：SVG 遗产节点网络
- 右侧：当前事件、行动点、合法动作、当前节点任务
- 下方：公开文化市场和当前玩家手牌
- 周边：玩家信息、节点状态、共同图卷

节点必须显示：

- 名称
- 损伤 0/3
- 当前任务进度
- 所在玩家
- 是否可移动到
- 是否可修护/探索/贡献

连接边和可移动目标由服务端状态高亮。

### 8.2 交互流程

- 点击行动按钮后，再高亮合法目标；或点击节点后显示上下文动作。
- 不允许玩家点击无效目标后才收到难懂错误。
- 所有异步动作有 loading、disabled 和失败恢复。
- 有 `pending_choice` 时锁定其他操作并显示选择 UI。
- 每个动作后只使用服务端返回的新状态重新渲染。
- 任务完成、事件结算、节点关闭和胜负有明确动画，但提供减少动画选项。

### 8.3 响应式与无障碍

必须修复审查报告中的移动端溢出风险：

- 390px 和 768px 不出现整页水平滚动。
- 不使用会反向撑宽容器的固定 `min-height`。
- 移动端可以把网络地图缩放为自适应 SVG，或切换为节点卡片列表。
- 关键点击目标不小于约 44×44 CSS px。
- 按钮和节点支持键盘操作。
- 节点、玩家棋子、损伤、任务进度提供 `aria-label`。
- 金色低对比颜色不得用于小号正文。
- 同一节点的多个玩家棋子必须有可辨识偏移或堆叠表示。

### 8.4 文化内容展示

文化说明使用“短解释 + 展开详情”，不要在主操作界面堆叠长文。

任务完成弹窗显示：

- 任务名称
- 本次组合了哪些文化领域/来源
- 80–150 字以内的说明
- 来源与审核状态
- “加入本局图卷”的视觉反馈

无可靠来源时明确显示“内容待专家审核”，不要伪造引用。

## 九、测试要求

保留并更新现有测试。至少覆盖：

1. 创建 2、3、4 人游戏。
2. 难度配置正确。
3. 节点图连接合法。
4. 非相邻移动被拒绝。
5. AP 正确扣除，AP 不得为负。
6. 玩家可提前结束回合。
7. 最后一名玩家结束后只结算一次事件。
8. 公开市场补牌正确。
9. 手牌上限触发弃牌选择。
10. 同节点交换合法；不同节点交换被拒绝。
11. 贡献卡牌与任务需求校验。
12. 多玩家共同贡献可完成任务。
13. 完成任务更新图卷、印记、资源和日志。
14. 修护不能低于 0，资源不能低于 0 或超过上限。
15. 四个角色技能每回合只能使用一次。
16. 事件预告与结算顺序正确。
17. 节点达到阈值后关闭。
18. 两个节点关闭触发失败。
19. 五印记 + 10/10 图卷触发胜利。
20. 轮数耗尽触发失败。
21. SQLite 保存、重载后状态一致。
22. 固定 seed 得到确定性牌序和事件序。
23. 相同 revision 的重复请求只有一次成功，陈旧请求返回 409。
24. 旧 schema 存档得到明确的 legacy 响应。
25. 所有文化内容引用字段通过 schema 校验。

测试应尽量针对纯领域函数，不要全部依赖 HTTP。

## 十、浏览器审查与验收

更新 `scripts/audit_screenshots.py` 或现有 Playwright 流程，使其可以：

1. 创建普通难度游戏。
2. 选择角色。
3. 完成移动、探索、贡献、修护、结束回合。
4. 截取：
   - 1920×1080
   - 1440×900
   - 1280×800
   - 768×1024
   - 390×844
5. 检测：
   - 页面水平溢出
   - 按钮重复提交
   - 当前玩家与 AP 是否清晰
   - 节点损伤和任务进度是否可见
   - 手牌/市场是否遮挡
   - 弹窗是否超出视口

若项目已安装或可轻量加入 axe-core，则运行无障碍检查；否则至少保留语义和键盘测试。

## 十一、文档交付

更新或新增：

- `README.md`
- `ARCHITECTURE.md`
- `MECHANICS.md` 或 `MECHANICS_V2.md`
- `CODEX_TASKS.md`
- `docs/LEARNING_MECHANICS_MAP.md`
- `docs/MIGRATION_V1_TO_V2.md`
- `docs/legacy/MECHANICS_V1_FLYING_CHESS.md`
- API 请求示例
- 新版 3 分钟答辩讲解流程

新版规则文档必须避免继续使用：

- 掷骰
- 飞行棋
- 前进若干格
- 落点格子
- 个人集齐五印记胜利

可以保留“飞天”作为文化主题、卡牌名称或美术意象，但不能再作为“飞行移动机制”的术语。

## 十二、分阶段实施优先级

### P0：必须完成

- 领域状态模型 v2
- 节点网络移动
- 3 AP 行动系统
- 公开文化市场与手牌
- 节点任务共同贡献
- 节点损伤与修护
- 回合事件预告与结算
- 新胜负条件
- 四角色新版技能
- 统一动作 API
- revision 防重复提交
- SQLite v2 持久化
- 新主界面
- 核心测试
- 390/768 无水平溢出

### P1：完成 P0 后处理

- 三档难度
- 文化来源详情与终局资料汇总
- Playwright 多视口审查
- axe 检查
- 图像 WebP/AVIF 优化
- 更完整动画和音效开关

### P2：只记录待办，不在本次强行实现

- 单人 AI
- 战役解锁
- React/Vite
- WebSocket
- AR/语音
- 实体桌游打印文件

## 十三、最终验收标准

只有同时满足以下条件，才视为重构完成：

1. 主界面没有骰子和 48 格顺序赛道。
2. 玩家通过节点网络和 AP 做选择，机制上明确不是飞行棋。
3. 一局可从创建、行动、事件、任务一直运行到胜利或失败。
4. “文明互鉴”通过不同来源卡牌组合与玩家协作实际发生。
5. “遗产保护”通过节点损伤、修护资源和事件压力实际发生。
6. 服务端返回合法动作，前端不复制规则。
7. 重复点击不会导致重复结算。
8. SQLite 重载可继续游戏。
9. 自动测试通过。
10. 390px、768px、1280px、1920px 主要界面可用，无整页水平溢出。
11. 旧版文档和回退路径仍可找到。
12. 没有编造未审核的文化事实或来源。

## 十四、完成后的输出格式

完成代码修改后，给出：

1. 重构摘要。
2. 关键设计决定及原因。
3. 修改文件列表。
4. 数据结构和 API 变化。
5. 测试命令与实际结果。
6. Playwright 截图路径和审查结果。
7. 旧存档处理方式。
8. 尚未完成的 P1/P2 项目。
9. 一套从新建游戏到触发一次任务完成和一次事件结算的手动验收步骤。

不要只给代码片段。请实际修改仓库、运行测试、修复失败，并把项目保持在可启动状态。
