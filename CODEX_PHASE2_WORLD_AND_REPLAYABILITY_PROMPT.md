# Codex 第二阶段任务：把《石窟光谱》扩展为全屏、宏大、可重复游玩的文化策略游戏

仓库：`https://github.com/future3317/yungang`

本任务承接已经完成的第一阶段修正：边线去重、地图拖动、`legal_actions` 可达状态、动作模式、元数据驱动、中文正文、409 自动同步、真实连接状态和结算原因。不要回退这些改动。

开始前先确认截图对应的代码分支。当前 GitHub `main` 可能仍是旧版大组件；运行：

```bash
git status
git branch --show-current
git log -5 --oneline
```

在包含第一阶段修正的分支创建检查点：

```text
chore: checkpoint phase-1 interaction fixes
```

然后实际修改、运行测试并提交可运行版本，不要只写设计方案。

---

## 1. 本阶段目标

当前版本有两个根本问题：

1. 地图仍是三栏页面中间的一块面板，六个小节点漂浮在大面积空背景里，整体仍像深色 Dashboard。
2. 内容量和规则组合过少，固定节点、固定牌序、固定事件和单一胜利条件无法支撑重复游玩。

本阶段同时完成：

- **全屏遗产世界地图**
- **中文排版与字体重做**
- **18–24 节点的内容架构**
- **情景、随机开局、多阶段项目、路线建设、角色成长**
- **不同胜负条件与终局复盘**

产品定位：

> 玩家进入一张会因为探索、修护、事件和协作而改变的“大同文化网络”，而不是操作六个按钮。

---

## 2. 先做审计

新增 `docs/audit/PHASE2_GAP_AUDIT.md`，记录：

- 截图版本与 GitHub main 的差异；
- 1440×900 下地图占屏比例；
- 小于 14px 的中文文字；
- 面板和空白占比；
- 当前节点、路线、卡牌、事件、任务数量；
- 当前随机化程度；
- 当前胜负条件；
- 内容扩张的代码阻塞点。

审计后继续实现。

---

## 3. UI：地图成为整个游戏世界

### 3.1 桌面布局

1440×900 和 1920×1080 下采用：

```text
顶部状态带 56–64px
全屏地图舞台
  ├─ 左侧可折叠玩家浮层
  ├─ 右侧上下文抽屉
  └─ 地图节点、路线、事件覆盖
底部行动坞 104–132px
```

要求：

- 地图占主要内容区域至少 75%；
- 地图高度为 `calc(100dvh - header - dock)`；
- 取消固定三栏挤压地图；
- 任务、事件、市场和日志不永久占据地图宽度；
- 点击节点时才展开右侧详情；
- 空手牌不占大块区域；
- 时间线放进抽屉；
- 主要决策在 1440×900 下不需要纵向滚动。

### 3.2 地图三层语义缩放

**Level 0：文化区域**

至少四区：

- 平城都城网络
- 云冈石窟与造像网络
- 寺观营造网络
- 边塞与互市网络

区域是游戏分类。未经审核的文化表述必须标记 `needs_review`。

**Level 1：主要节点**

标准情景显示 12–18 节点，完整情景最多 24 节点。保留现有六个锚点，其他节点分为：

- 已审核真实节点；
- 明确标注为“游戏化主题节点”的工坊、档案、材料、行旅、修护等节点。

**Level 2：节点面向**

聚焦节点后显示 2–4 个面向：

- 造像与图像
- 纹样与色彩
- 建筑与材料
- 文献与记忆
- 路线与交流
- 风化与修护

任务作用于面向，不再所有节点只显示一个 `0/3`。

### 3.3 地图实现

保留自定义 SVG，不使用 React Flow。建议加入：

```text
d3-zoom
d3-selection
```

实现：

- 滚轮、触控板、拖动和按钮缩放；
- `fitToWorld()`；
- `focusNode(id)`；
- 语义缩放；
- 节点文字保持可读；
- 移动端节点列表等价操作；
- 节点和路线来自 meta；
- 状态来自服务端；
- 普通、可达、阻断、已修复、已连接、事件影响路线；
- 普通路线低对比，仅决策相关路线高亮。

中央佛像作为视觉核心，路线不得穿过主体面部；使用遮罩、曲线路径或绕行。节点区分核心遗产、支撑节点、临时事件节点三种形态。

### 3.4 字体与字号

使用合法本地 WOFF2 子集并保留许可证：

- Noto Sans SC Variable：界面和正文；
- Noto Serif SC Variable：标题和文化说明；
- 等宽字体只用于少量数字。

桌面字号：

- 展示标题 48–64px；
- 一级标题 36–48px；
- 节点标题 18–22px；
- 面板标题 18–24px；
- 正文 16px；
- 次要正文 14px；
- 标签 12–13px；
- 功能性中文禁止 9–11px。

中文为主，英文只作辅助。正文行高至少 1.65。低对比金色不得用于小字。完成 WCAG 2.2 AA 对比度检查。

### 3.5 降低 Dashboard 感

- 不给所有区域加完整矩形边框；
- 用空间、排版、材质和局部光线分层；
- 玩家是“编队名册”；
- 任务是“石刻档案页”；
- 事件是“环境观测条”；
- 手牌是“器物托盘”；
- 减少永久面板数量。

---

## 4. 内容扩张目标

首个扩展版本至少包含：

- 4 个区域；
- 18 个主要节点；
- 24–36 条路线；
- 36 张文化证据牌；
- 12 张技艺/行动牌；
- 18 张事件牌；
- 12 个普通任务；
- 6 个多阶段项目；
- 8 个公共目标；
- 4 个标准情景；
- 4 个角色、每角色 2 个成长方向；
- 12 个终局称号。

未审核文化正文必须使用：

```json
{
  "content_review_status": "placeholder",
  "source_title": "",
  "source_url": "",
  "source_note": "TODO_CONTENT_REVIEW"
}
```

Codex 不得伪造史实、来源或审核结果。

新增数据文件：

```text
data/
  regions.json
  sites.json
  site_facets.json
  routes.json
  scenarios.json
  task_templates.json
  projects.json
  culture_cards.json
  action_cards.json
  events.json
  event_chains.json
  roles.json
  role_upgrades.json
  objectives.json
  achievements.json
  terminology.json
  difficulty.json
```

`terminology.json` 必须提供 domain、origin、status、action 和资源的中文名、图标、色彩 token 与简短说明。前端不再显示 `architecture`、`central`、`at_risk` 等内部代码。

---

## 5. 耐玩性系统

### 5.1 情景系统

实现至少四个实质不同的情景：

1. **风沙与石**：风化压力高，重点是修护与路线保全。
2. **文明汇流**：强调不同来源组合和跨区交换。
3. **互市重开**：多条路线初始阻断，需要勘察和恢复。
4. **万象成卷**：长局，需要完成多区域项目和最终核心任务。

每个情景必须改变：

- 地图节点池；
- 路线初始状态；
- 事件构成；
- 任务池；
- 初始资源；
- 公共目标；
- 胜利与失败条件。

不能只调整回合数。

### 5.2 Seed 和确定性随机

新增并持久化：

- seed；
- RNG position；
- 牌库洗牌；
- 情景节点抽样；
- 任务抽样；
- 事件牌堆构建；
- 初始损伤和路线阻断。

创建游戏支持：

- 随机 seed；
- 自定义 seed；
- 每日旅程 seed；
- 再玩同一 seed。

相同 seed、情景和人数必须得到同一开局。不要继续使用固定卡牌和固定事件顺序。

### 5.3 多阶段项目

实现：

**调查项目**

1. 发现线索
2. 比较证据
3. 完成解释

**修护项目**

1. 诊断损伤
2. 准备材料
3. 执行修护
4. 记录成果

**连接项目**

1. 勘察路线
2. 建立协作
3. 恢复线路

项目完成后必须真实改变地图、路线、资源或任务，而不只是增加进度数字。

### 5.4 路线状态

路线成为状态实体：

```json
{
  "id": "route_x",
  "from": "site_a",
  "to": "site_b",
  "cost": 1,
  "status": "open",
  "risk": 0,
  "connection_level": 0,
  "active_project_id": null
}
```

状态：

- open
- strained
- blocked
- restored
- illuminated

新增动作：

- `survey_route`
- `restore_route`
- `establish_connection`

路线完成连接后可以：

- 降低移动成本；
- 提供市场刷新；
- 提高来源多样性；
- 减少事件损伤；
- 增加共同影响。

### 5.5 资源

只保留三个核心资源：

1. 个人 AP；
2. 团队修护资源；
3. 研究线索。

共同影响继续作为进度，不作为普通消费资源。

新增 `prepare`：保存 1 AP 为准备标记，可在回合末响应事件，形成“当前行动与未来风险”的权衡。

### 5.6 每轮流程

1. 揭示事件预告；
2. 团队规划；
3. 玩家依次行动；
4. 使用准备标记响应；
5. 事件结算；
6. 更新节点和路线；
7. 检查项目、目标和胜负。

规划阶段允许玩家放置计划标记，但不强制执行。

### 5.7 角色成长

保留四个角色，每局完成首次贡献或项目后，从两个专长中选择一个：

- 平城工匠：石作精修 / 营造统筹
- 西域乐舞使者：和鸣 / 传播
- 草原骑行使者：疾行 / 路勘
- 中原文书记官：博览 / 编目

成长必须改变行动策略，而不是简单数值 +1。

### 5.8 卡牌

分为：

- 文化证据牌；
- 技艺牌；
- 协作牌；
- 关键档案。

文化证据包含：

- domain；
- origin；
- site_tags；
- technique_tags；
- era_tags；
- rarity；
- culture_note；
- source。

实现数据化组合：

- 不同 origin → 互鉴；
- 同 domain + 不同 origin → 对照；
- architecture + trade → 营造流通；
- statue + pattern → 图像复原；
- frontier + trade → 边塞交流。

组合可推进项目、产生线索、点亮路线或解锁文化解释。

### 5.9 公共目标与胜负

每个情景从目标池抽取 2–3 个：

- 完成多个区域项目；
- 建立贯通网络；
- 完成跨来源组合；
- 保护核心节点；
- 修复受损路线；
- 收集指定档案。

失败条件也按情景变化：

- 关闭节点过多；
- 网络断裂；
- 区域风险过高；
- 回合耗尽；
- 资源崩溃。

服务端返回明确：

```text
outcome
outcome_reason
objective_progress
```

### 5.10 终局复盘

合作胜负之外增加 0–100 团队评分：

- 任务；
- 路线；
- 多样性；
- 节点保护；
- 剩余资源；
- 回合效率；
- 内容发现。

结算显示：

- 铜 / 银 / 金 / 完整光谱；
- 玩家贡献画像；
- 关键组合；
- 最危险时刻；
- 未完成目标；
- seed；
- 再玩同 seed；
- 新随机旅程。

不要增加互相冲突的个人胜利。

---

## 6. 行动交互

底部行动坞只显示动作类型：

- 移动；
- 调查；
- 探索；
- 贡献；
- 修护节点；
- 勘察路线；
- 修护路线；
- 交换；
- 准备；
- 技能；
- 结束回合。

流程：

1. 选择动作类型；
2. 地图、卡牌或玩家进入目标选择；
3. 右侧显示结果预览；
4. 确认；
5. 服务端结算。

显示 AP、资源成本、预期变化和不可执行原因。

后端不得直接把英文错误显示到 UI。统一错误结构：

```json
{
  "code": "site_does_not_need_restoration",
  "message": "当前节点没有需要修护的损伤",
  "details": {}
}
```

市场只在“探索”模式展开，手牌只在“贡献/使用”模式展开。卡牌显示它能满足当前项目的哪些条件。

---

## 7. 前后端架构

### 7.1 前端拆分

`GamePage.tsx` 只负责页面编排。至少拆出：

```text
ScenarioHeader
WorldMap
PlayerRoster
NodeInspector
ProjectPanel
EventForecast
CommandDock
CultureMarket
HandTray
PlanningOverlay
JourneyTimeline
ResultSummary
```

TanStack Query 管理服务端状态；Zustand 或 reducer 只管理：

- 地图 transform；
- 聚焦节点；
- 动作模式；
- 选择目标；
- 抽屉；
- 卡牌筛选；
- 偏好。

加入：

- Radix Dialog；
- Radix Tabs；
- Radix Tooltip；
- Radix ScrollArea；
- Storybook；
- MSW；
- axe。

### 7.2 后端 schema v3

新增：

```text
schema_version = 3
```

为 Scenario、Region、SiteFacet、RouteState、ProjectState、ObjectiveState、RoleUpgrade、Card、Event、Action、ActionTarget、OutcomeReason 和 ErrorResponse 建立 Pydantic 模型。

拆分引擎：

```text
backend/domain/
  setup.py
  turn.py
  selectors.py
  outcome.py
  rng.py
  actions/
  effects/
  scenarios/
```

服务端返回聚合动作：

```json
{
  "type": "move",
  "label": "移动",
  "cost": {"ap": 1},
  "targets": [
    {
      "id": "site_x",
      "label": "前往节点",
      "preview": {
        "route_status": "open",
        "risk": 1
      }
    }
  ],
  "disabled_reason": null
}
```

前端不再把几十个扁平 `legal_actions` 自己分组和猜测。

V2 存档必须明确迁移或标为 legacy，不得静默损坏。

---

## 8. 测试

必须覆盖：

- 四个情景；
- 相同 seed 相同开局；
- 不同 seed 有变化；
- 重载保持 RNG；
- 多阶段项目；
- 路线阻断和恢复；
- 研究线索；
- prepare 响应；
- 角色成长；
- 公共目标；
- 多种胜负；
- 评分；
- outcome reason；
- V2 存档。

Playwright 完整执行：

- 创建情景；
- 地图缩放；
- 聚焦区域和节点；
- 移动；
- 调查；
- 贡献；
- 修护节点；
- 修护路线；
- 角色成长；
- 事件响应；
- 结算；
- 再玩同 seed。

视口：

- 1920×1080；
- 1440×900；
- 1280×800；
- 768×1024；
- 390×844。

检查地图占屏、中文字号、面板遮挡、横向溢出、焦点、对比度和 reduced motion。

---

## 9. 分阶段实现

### Phase A：视觉空间

- 全屏地图；
- 浮动玩家面板；
- 上下文抽屉；
- 新行动坞；
- 字体和字号；
- 地图 LOD；
- 中文错误。

### Phase B：内容架构

- region；
- scenario；
- facet；
- project；
- terminology；
- objective；
- role upgrade；
- 强类型校验。

### Phase C：首批内容

- 18 节点；
- 36 文化牌；
- 12 行动牌；
- 18 事件；
- 12 普通任务；
- 6 多阶段项目；
- 4 情景；
- 8 角色专长；
- 8 公共目标。

### Phase D：耐玩性

- seed；
- 洗牌；
- 地图/任务抽样；
- 事件构筑；
- 路线机制；
- 角色成长；
- 多目标；
- 评分；
- 再玩同 seed。

### Phase E：证据

- Storybook；
- Vitest；
- Playwright；
- axe；
- 视觉回归；
- Lighthouse；
- 多视口截图。

---

## 10. 非目标

本阶段不要做：

- WebSocket；
- 在线账号；
- 复杂 AI；
- 3D/WebGL；
- AR；
- 语音；
- 商业支付；
- 为数量加入没有决策作用的节点；
- 编造文化资料。

节点数量增加必须同步增加路线、任务、事件、决策和文化内容。

---

## 11. 验收标准

1. 地图是页面绝对主舞台；
2. 1440×900 下地图占主要视口；
3. 不再固定三栏挤压地图；
4. 主要正文 16px、次要正文至少 14px；
5. 不存在 9–11px 功能性中文；
6. 内部枚举全部中文化；
7. 不显示英文后端错误；
8. 至少 4 区、18 节点；
9. 有语义缩放；
10. 有 4 个实质不同情景；
11. 有 seed 和确定性随机；
12. 每局地图、任务、事件和牌序可变化；
13. 有多阶段项目；
14. 路线有状态和建设；
15. 有研究线索和 prepare；
16. 有角色成长；
17. 有公共目标和不同胜负；
18. 有终局评分和复盘；
19. 服务端仍是规则唯一来源；
20. `GamePage` 不再是巨型组件；
21. 内容强类型校验通过；
22. V2 存档有明确处理；
23. Playwright 全旅程通过；
24. axe 无 serious/critical；
25. 未伪造文化审核；
26. 提供 5 分钟演示脚本和标准局验收步骤。

---

## 12. 最终回复格式

1. 工作分支与 checkpoint；
2. 视觉和字体改动；
3. 地图层级；
4. 新内容数量；
5. 四个情景；
6. 新机制；
7. 数据 schema；
8. 前端组件；
9. 后端变化；
10. 测试实际结果；
11. 截图路径；
12. axe/Lighthouse；
13. 未审核内容；
14. 手动验收；
15. 未完成内容和风险；
16. 回退方式。

不要只写计划。实际修改代码、扩充数据、运行测试、修复失败并提交可运行版本。
